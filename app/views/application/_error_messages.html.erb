<% if object && object.errors.size > 0 %>
  <div class="alert alert-danger">
    <p>There were <%= object.errors.size %> problems with the following fields:</p>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
